import { Layout } from 'antd';

const { Footer } = Layout;

const MainFooter = () => {
  return (
    <Footer className='mt-auto'>
      <div className='container mx-auto text-center'>BlogApp</div>
    </Footer>
  );
};

export default MainFooter;
